# Copy to .env (gitignored). Names only — values live in 1Password.
# Inject at runtime: op run --env-file=.env -- <cmd>   OR   op inject -i .env.tpl -o .env
# Reference style:  EXAMPLE_TOKEN=op://DEV/<Item>/<field>
EXAMPLE_API_TOKEN=
