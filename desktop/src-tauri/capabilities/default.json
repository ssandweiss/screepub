{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "This app's own two commands, plus scoped doors: the window may ask the OS to open the project's issue tracker and the three Calibre and Kindle Previewer download pages the KFX checklist links to, to reveal a file inside the Screepub library, and to ask the updater plugin to check, download and install. The updater grant has no allow-list to write; its scope is the one endpoint in tauri.conf.json. Nothing else. The shell and dialog plugins are still used only from Rust. See ADR 2026-09-21 — doors, not commands.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "updater:default",
    "process:allow-restart",
    "core:window:allow-start-dragging",
    {
      "identifier": "opener:allow-open-url",
      "allow": [
        { "url": "https://github.com/ssandweiss/screepub/*" },
        { "url": "https://calibre-ebook.com/download_osx" },
        { "url": "https://calibre-ebook.com/download_windows" },
        { "url": "https://kdp.amazon.com/en_US/help/topic/G202131170" }
      ]
    },
    {
      "identifier": "opener:allow-reveal-item-in-dir",
      "allow": [{ "path": "$DOCUMENT/Screepub/**" }]
    }
  ]
}
