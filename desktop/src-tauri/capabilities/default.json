{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "This app's own two commands, plus scoped doors: the window may ask the OS to open the project's issue tracker, and to reveal a file inside the Screepub library. Nothing else. The shell and dialog plugins are still used only from Rust. See ADR 2026-09-21 — doors, not commands.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    {
      "identifier": "opener:allow-open-url",
      "allow": [{ "url": "https://github.com/ssandweiss/screepub/*" }]
    },
    {
      "identifier": "opener:allow-reveal-item-in-dir",
      "allow": [{ "path": "$DOCUMENT/Screepub/**" }]
    }
  ]
}
