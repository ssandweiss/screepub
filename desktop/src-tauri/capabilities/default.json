{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "This app's own two commands, plus ONE scoped door: the window may ask the OS to open the project's issue tracker, and nothing else. The shell and dialog plugins are still used only from Rust. See ADR 2026-09-21 — doors, not commands.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    {
      "identifier": "opener:allow-open-url",
      "allow": [{ "url": "https://github.com/ssandweiss/screepub/*" }]
    }
  ]
}
