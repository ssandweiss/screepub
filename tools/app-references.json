{
  "$comment": "The blast radius of deleting app/ \u2014 every tracked file OUTSIDE app/ that names the SwiftUI app, its directory, its build commands or the release artifacts only it produces. Enforced by tests/app-references.test.ts, which defines what counts as a reference and why. `refs` is a CEILING: it may fall (that is progress toward piece F3), never rise without a row being updated deliberately. `category` is what would break: build = a workflow or script that executes or packages app/; test = a bun test that reads app/ or asserts about its artifacts; artifact = names a release artifact or bundle only the Swift app produces; doc = user-facing prose that becomes false; note = a comment citing app/ as provenance, stale rather than broken; freeze = the F1 freeze machinery itself. See docs/superpowers/specs/2026-09-14-retire-swiftui-design.md (F3 acceptance criterion 11: this file is empty when the retirement is done).",
  "files": {
    ".github/dependabot.yml": {
      "refs": 2,
      "category": "note",
      "breaks": "A comment explaining why there is no SwiftPM ecosystem entry: it names app/Package.swift. Goes with the directory."
    },
    ".github/workflows/ci.yml": {
      "refs": 6,
      "category": "build",
      "breaks": "The `app` job: swift build + `swift run kit-check` on macos-15, working-directory: app. This job is COVERAGE (171 of kit-check's 264 checks exist nowhere else) and must survive until F3, then be deleted whole."
    },
    ".github/workflows/release.yml": {
      "refs": 28,
      "category": "build",
      "breaks": "The macOS Swift job (working-directory: app), app/release.sh, the three app/dist assets it uploads, and the tap crib sheet that hashes them. ALSO, since 2026-09-21, the notarization step's comment cites app/release.sh as the reference for notarize+staple on the DMG: that is provenance, not a dependency, and the step keeps working when app/ goes. NOTE for whoever greps: this file ALSO says `path: app` and `cd app` in the app-bundles/app-upload jobs, and those are the TAURI bundle staging directory, not this repository directory. Do not delete them."
    },
    ".github/workflows/weekly-toolchain.yml": {
      "refs": 5,
      "category": "build",
      "breaks": "The ENTIRE workflow is `swift run -c release kit-check` with working-directory: app, on macos-15 with Calibre. It has no non-Swift job, so F3 deletes the file outright. It is reachable only through `working-directory: app`, which a grep for \"app/\" does not see."
    },
    ".gitignore": {
      "refs": 3,
      "category": "build",
      "breaks": "Ignores app/build/, app/dist/, app/.build/ \u2014 the SwiftPM and release output paths."
    },
    "CLAUDE.md": {
      "refs": 16,
      "category": "doc",
      "breaks": "The architecture section, both Swift build commands, and the format-defaults.json invariant that says BOTH options.test.ts and kit-check pin it (F3 acceptance criterion 14 rewrites that sentence)."
    },
    "CONTRIBUTING.md": {
      "refs": 8,
      "category": "doc",
      "breaks": "The contributor build commands: app/build-app.sh and `swift run kit-check`, plus the description of CI's app job."
    },
    "README.md": {
      "refs": 12,
      "category": "doc",
      "breaks": "The DMG download button, \"the macOS app in app/ remains the supported Mac app\", the build commands, and the source-tree map. User-facing: it must not point at a directory that is gone."
    },
    "SECURITY.md": {
      "refs": 1,
      "category": "doc",
      "breaks": "Names app/screepub-engine.entitlements in the threat-model section."
    },
    "THIRD-PARTY-NOTICES.md": {
      "refs": 11,
      "category": "doc",
      "breaks": "Two live paths inside app/Packages/KFXKit (PROVENANCE.md and LICENSE) plus the Screepub.app resource path. This is a LICENSE document: a dangling path in it is a compliance defect, not a typo. F3 acceptance criterion 13."
    },
    "brand/README.md": {
      "refs": 3,
      "category": "doc",
      "breaks": "Declares app/Sources/ScreepubApp/Theme.swift the source of the shared brand tokens. Reversed by F3 criterion 12."
    },
    "brand/tokens.css": {
      "refs": 2,
      "category": "note",
      "breaks": "Comments saying which values mirror Theme.swift."
    },
    "brand/tokens.json": {
      "refs": 11,
      "category": "test",
      "breaks": "`\"from\": \"Theme.swift\"` on eight tokens, plus a $comment naming the Swift path. This is the machine-readable half of the pin that tests/brand-tokens.test.ts enforces; promoting this file to source of truth is F3 criterion 12."
    },
    "desktop/README.md": {
      "refs": 1,
      "category": "artifact",
      "breaks": "Names Screepub.app \u2014 the bundle name the Tauri app takes over at F2."
    },
    "desktop/ui/notes.js": {
      "refs": 1,
      "category": "artifact",
      "breaks": "GENERATED by tools/build-desktop-notes.ts from docs/releases/<package.json version>.md, which is why this row appeared at 0.6.0 and not before: 0.6.0's notes say the Mac app has not changed and name Screepub-macOS.dmg, so the window now shows that sentence. docs/releases/ is excluded from this sweep as a dated record, but this file is not a record \u2014 it is what the running app displays, so the prose goes stale in front of a user rather than in an archive. Regenerating from a later release file is what removes it; delete this row when that happens."
    },
    "desktop/ui/style.css": {
      "refs": 1,
      "category": "note",
      "breaks": "A comment naming Theme.swift as what the token values are re-checked against."
    },
    "desktop/ui/tune.js": {
      "refs": 3,
      "category": "note",
      "breaks": "A comment crediting the Mac app's rail (app/Sources/ScreepubApp/) as the origin of the tune panel."
    },
    "docs/device-map.md": {
      "refs": 2,
      "category": "doc",
      "breaks": "\"Current support (app code, verified against ScreepubKit)\" is the provenance line for the support table; it stops being a checkable claim at F3."
    },
    "docs/formatting-options-log.md": {
      "refs": 13,
      "category": "doc",
      "breaks": "The options registry names Swift files as the implementing code for three behaviours and kit-check as their guard. CLAUDE.md calls this file the app's options registry, so a stale entry here misleads by design."
    },
    "docs/mac-qa.md": {
      "refs": 6,
      "category": "note",
      "breaks": "The gate-1 QA checklist. It names Screepub.app and the flat ~/Documents/Screepub layout because its whole job is telling a tester how the shipping app and its replacement differ while BOTH are installed. The fifth reference, added 2026-09-14, is UpdateInstall.swift: the checklist now records that the updater refusal is structural rather than predicted, because the identifier pin is an exact match, and points at the ADR that decides what F2 ships because of it. At F2 the coexistence sections stop applying; at F3 the file should go, since the gate it describes will have been passed, and this reference goes with it because the ADR it points to outlives the file."
    },
    "docs/release-notes-template.md": {
      "refs": 5,
      "category": "doc",
      "breaks": "A live instruction to the notes author: edit app/Sources/ScreepubApp/ReaderRail.swift when a bullet announces a UI change. Not a record \u2014 a procedure that would tell someone to edit a deleted file."
    },
    "docs/parity-audit.md": {
      "refs": 18,
      "category": "doc",
      "breaks": "The audit IS an inventory of what only app/ has, so it names ResultActions.swift, ContentView.swift, ReaderRail.swift, ScreepubApp.swift and KitCheck by path and line. Every reference is the point of the file rather than a dependency on it. It is also the file F3 has to be able to answer: this row goes when the audit is empty, which is the same moment app/ can be deleted."
    },
    "docs/release-secrets.md": {
      "refs": 2,
      "category": "doc",
      "breaks": "Says app/release.sh computes the three SHAs the tap bump needs, and names Screepub-macOS.dmg among the re-uploaded assets."
    },
    "docs/retired-coverage.md": {
      "refs": 54,
      "category": "note",
      "breaks": "The decision record for the 171 kit-check assertions that have no bun test counterpart. It cites Swift sources and kit-check sections by name throughout, because naming what is lost is its whole job. The 52nd reference, added 2026-09-14, is UpdateInstall.swift in the note recording that the owner chose a FULL ported self-update: that moved self-update-installer's 26 checks from accept-loss to port, so the file now names the Swift source those checks have to be reproduced FROM rather than merely mourned. The 53rd and 54th, added 2026-09-20, are in the kfx-install-plugin row: it now names app/Packages/KFXKit's vendored zip and its PROVENANCE.md in order to say that we STOPPED shipping them, which is a reference that exists to record a removal. It OUTLIVES app/: at F3 its rows stop describing code that exists and start describing what was given up, which is the record a later maintainer needs. Do not delete it with the directory."
    },
    "site/index.html": {
      "refs": 4,
      "category": "artifact",
      "breaks": "Three DOWNLOAD FOR MACOS buttons pointing at Screepub-macOS.dmg plus the supported-download paragraph. The count of three is itself pinned by tests/release-artifacts.test.ts."
    },
    "src/cli-devices.ts": {
      "refs": 1,
      "category": "note",
      "breaks": "A comment pointing at RemarkableDevice.swift for the byte-identical behaviour."
    },
    "src/device/remarkable.ts": {
      "refs": 4,
      "category": "note",
      "breaks": "A comment that explicitly asks NOT to be reworded \"while app/ exists\", because KitCheck still runs against it. It becomes free to edit at F3 \u2014 and should be, or it will keep pointing readers at nothing."
    },
    "src/device/volumes.ts": {
      "refs": 1,
      "category": "note",
      "breaks": "Records a deliberate divergence from Device.swift."
    },
    "src/export/artifact.ts": {
      "refs": 1,
      "category": "note",
      "breaks": "A comment citing Export.swift's behaviour as the precedent."
    },
    "src/export/calibre.ts": {
      "refs": 2,
      "category": "note",
      "breaks": "Two comments: one saying the two-step matches EbookConvert.swift exactly, one recording a deliberate divergence from it."
    },
    "src/export/kfx.ts": {
      "refs": 1,
      "category": "note",
      "breaks": "A comment citing Export.swift's mobiSibling(for:)."
    },
    "src/settings/presets.ts": {
      "refs": 1,
      "category": "note",
      "breaks": "Repeats the claim that options.test.ts AND kit-check both pin format-defaults.json. That sentence becomes false at F3 (criterion 14), in source rather than in a doc."
    },
    "tests/brand-components.test.ts": {
      "refs": 2,
      "category": "note",
      "breaks": "A comment crediting Theme.swift/ContentView.swift as where the components existed first."
    },
    "tests/brand-tokens.test.ts": {
      "refs": 9,
      "category": "test",
      "breaks": "FAILS OUTRIGHT once Theme.swift is gone: it filters brand/tokens.json for from:\"Theme.swift\" and compares each value against the parsed Swift. F3 criterion 12 reverses its direction."
    },
    "tests/build-app-bundle.test.ts": {
      "refs": 3,
      "category": "test",
      "breaks": "Asserts the Tauri DMG is NOT named Screepub-macOS.dmg. The assertion loses its subject when the Swift DMG stops being published (F2), and the F2 rename of the Desktop infix rewrites it."
    },
    "tests/build-cli.test.ts": {
      "refs": 1,
      "category": "note",
      "breaks": "A comment siting the unsigned CLI \"alongside the signed one app/release.sh builds\"."
    },
    "tests/cli.test.ts": {
      "refs": 1,
      "category": "note",
      "breaks": "A comment saying the --json sample is the same file kit-check decodes."
    },
    "tests/desktop-shell.test.ts": {
      "refs": 13,
      "category": "test",
      "breaks": "Pins the bundle-identifier split against app/Sources/KitCheck/main.swift, and holds a whole \"nothing under app/ or src/ was drawn into this\" boundary describe block. Its subject moves at F2 and several assertions go at F3."
    },
    "tests/device-kindle.test.ts": {
      "refs": 1,
      "category": "note",
      "breaks": "A comment saying tempDir() mirrors kit-check's."
    },
    "tests/device-remarkable.test.ts": {
      "refs": 2,
      "category": "note",
      "breaks": "A comment saying the request recorder mirrors kit-check's StubRemarkable."
    },
    "tests/device-transfer.test.ts": {
      "refs": 1,
      "category": "note",
      "breaks": "A comment saying tempDir() mirrors kit-check's."
    },
    "tests/export-calibre.test.ts": {
      "refs": 6,
      "category": "note",
      "breaks": "Cites KitCheck/main.swift:349-350 BY LINE NUMBER. Line citations into a directory scheduled for deletion rot in both directions; this is the one to rewrite first."
    },
    "tests/export-kfx.test.ts": {
      "refs": 1,
      "category": "note",
      "breaks": "A comment citing Export.swift's mobiSibling(for:) precedent."
    },
    "tests/frozen-app.test.ts": {
      "refs": 31,
      "category": "freeze",
      "breaks": "The freeze guard itself. It reads app/ and app/README-FROZEN.md, so F3 deletes it in the same commit as the directory."
    },
    "tests/mobi.test.ts": {
      "refs": 1,
      "category": "note",
      "breaks": "A comment citing Export.swift's kfxReady."
    },
    "tests/options.test.ts": {
      "refs": 1,
      "category": "note",
      "breaks": "A comment saying kit-check decodes the same format-defaults.json."
    },
    "tests/release-artifacts.test.ts": {
      "refs": 14,
      "category": "test",
      "breaks": "READS app/release.sh and asserts it still produces the two tarballs and the DMG, that release.yml uploads app/dist/*, and that site/index.html carries exactly three DMG links. Roughly eight tests fail at F3; several must change at F2 instead."
    },
    "tests/release-app-step.test.ts": {
      "refs": 1,
      "category": "test",
      "breaks": "The notarization step's test names app/release.sh as the reference for the submit/staple/validate sequence it asserts. Provenance in a comment, not a dependency: the assertions are about release.yml's own bash and survive app/ being deleted."
    },
    "tests/sidecar-targets.test.ts": {
      "refs": 1,
      "category": "note",
      "breaks": "A comment naming app/release.sh as the other producer of the macOS targets."
    },
    "tests/sidecar.test.ts": {
      "refs": 1,
      "category": "note",
      "breaks": "A comment saying the assertion was ported verbatim from kit-check."
    },
    "tests/smoke-bundle.test.ts": {
      "refs": 3,
      "category": "artifact",
      "breaks": "Builds a fake DMG whose bundle is named Screepub.app. Harmless today; it collides in meaning once the Tauri bundle takes that name at F2."
    },
    "tests/theme-colors.ts": {
      "refs": 5,
      "category": "test",
      "breaks": "READS app/Sources/ScreepubApp/Theme.swift and parses it. The single hardest F3 dependency: it is the only file outside app/ that opens a file inside app/ for its CONTENT rather than its existence."
    },
    "tests/verify-signing.test.ts": {
      "refs": 12,
      "category": "test",
      "breaks": "Reads app/Sources/ScreepubKit/UpdateInstall.swift and pins verify-signing.ts's codesign requirement strings against it, clause by clause. This is the cross-pin that stops the release gate checking a paraphrase; it is also the thing that stops working the moment app/ is gone. At F3 the requirements have no upstream left, so either they become this repository's own definition with the Swift quoted in a comment, or the gate goes with the frozen updater it serves."
    },
    "tools/build-app-bundle.ts": {
      "refs": 3,
      "category": "artifact",
      "breaks": "The DMG naming rule, written specifically so the Tauri artifact cannot collide with Screepub-macOS.dmg. F2 removes the Desktop infix and this comment with it."
    },
    "tools/build-cli.ts": {
      "refs": 3,
      "category": "note",
      "breaks": "Comments explaining that the signed macOS CLI comes from app/release.sh instead."
    },
    "tools/build-sidecar.ts": {
      "refs": 1,
      "category": "note",
      "breaks": "A comment drawing the line between this tool's job and app/release.sh's."
    },
    "tools/bump-tap.sh": {
      "refs": 2,
      "category": "artifact",
      "breaks": "Hardcodes Screepub-macOS.dmg for the cask digest. TAP MACHINERY \u2014 belongs to gate 3 and must not be edited before F2 answers the per-arch question. Listed here so the blast radius is known, not so it is touched."
    },
    "tools/check-tap.sh": {
      "refs": 1,
      "category": "artifact",
      "breaks": "Verifies the published cask still serves Screepub-macOS.dmg. TAP MACHINERY, gate 3. It is run weekly by .github/workflows/tap-freshness.yml, which carries no textual reference of its own and is therefore INVISIBLE to this sweep \u2014 the second-order reference this row exists to record."
    },
    "tools/frozen-app-manifest.json": {
      "refs": 67,
      "category": "freeze",
      "breaks": "The frozen inventory of app/'s Swift files. F3 deletes it with the directory."
    },
    "tools/release-notes.ts": {
      "refs": 1,
      "category": "note",
      "breaks": "A comment naming src/ and app/ as the two trees a denylist would otherwise have had to enumerate."
    },
    "tools/sidecar-targets.ts": {
      "refs": 1,
      "category": "note",
      "breaks": "A comment saying the signed macOS CLI is notarized from app/release.sh."
    },
    "tools/verify-signing.ts": {
      "refs": 7,
      "category": "test",
      "breaks": "Carries UpdateInstaller's teamID, bundleID and both designated requirements, because the whole point is to check a published DMG against what the FROZEN app will actually demand of it. Its source of truth is a Swift file in app/. The notarization section added 2026-09-21 also cites app/release.sh and Screepub-macOS.dmg, as the reference behaviour and the comparison artifact: both become historical notes at F3 rather than breakage, since the checks themselves are about the Tauri DMG. Goes with tests/verify-signing.test.ts."
    },
    "tools/smoke-bundle.ts": {
      "refs": 1,
      "category": "artifact",
      "breaks": "A comment saying piece F renames \"Screepub Desktop.app\" to \"Screepub.app\"."
    }
  }
}
